<%= form_for(@report, :html => {:role => "form"}) do |f| %>
  <% if @report.errors.any? %>
    <div class="alert alert-danger">
      <strong><%= pluralize(@fishery.errors.count, "error") %> prohibited this report from being saved:</strong>
      <ul>
      <% @report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <label for="report_todays_fishing">Todays Fishing</label>
    <%= f.text_area :todays_fishing, class: "form-control", size: "24x6", placeholder: "Enter todays fishing report"%>
  </div>
  <div class="form-group">
    <label for="fishery_prospects">Prospects</label>
    <%= f.text_area :prospects, class: "form-control", id:"fishery_prospects", size: "24x6", placeholder: "What will tomorrow bring?"%>
  </div>
    <%= button_tag "Submit", class: "btn btn-default" %>
<% end %>
