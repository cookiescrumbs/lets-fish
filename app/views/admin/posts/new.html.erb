<%= render 'navigation' %>
  <section class="container p-top17">
    <div class="container">
      <div class="row">
        <div class="col-lg-8">
          <h3 class="m-bottom44">Adding a new post to the <%= @trip.title %> trip taken on the <%= @trip.formated_date %></h3>
          <%= render 'form' %>
        </div>
      </div>
    </div>
  </section>
