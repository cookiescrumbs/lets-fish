<%= render 'shared/navigation' %>
<section id="waters" class="bg-light-gray">
    <div class="container text-center">
        <div class="row">
            <div class="col-lg-12">
                <h3 class="section-heading">Trips</h3>
                <h3 class="section-subheading text-muted">A journal of our fishing trips for you to explore</h3>
            </div>
        </div>
        <div class="row">
            <%= render 'shared/trips' %>
        </div>
    </div>
</section>
<%= render 'shared/contributor', locals: { background_color: false }%>
